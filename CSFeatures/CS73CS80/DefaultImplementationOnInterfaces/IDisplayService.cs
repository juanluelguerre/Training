namespace DefaultImplementationOnInterfaces
{
    public interface IDisplayService
    {
        // Now in C# 8.0 we can create a default implementation.
        // void DisplayMessage(string message) { WriteLine(message); }

        // https://github.com/dotnet/csharplang/milestone/11
    }
}
